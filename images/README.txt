Drop your 3 real screenshots here:
- budget-input.png  (the color-coded input tab)
- dashboard.png     (the feedback dashboard)
- charts.png        (the visual charts)

Use PNG, ~1600px wide. They will replace the gray placeholders in index.html automatically if you keep these filenames.
Once added, delete this README.txt.
